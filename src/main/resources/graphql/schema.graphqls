type Person {
    id: ID!
    name: String!
    address: String!
}
# The Root Query for the application
type Query {
    findUsers(name: String): [Person]!
}

# The Root Mutation for the application
#type Mutation {
#    createPost(title: String!, text: String!, category: String, authorId: String!) : Post!
#}